# =============================================================================
# GOOGLE GEMINI API CONFIGURATION
# =============================================================================
# Get your API key from: https://aistudio.google.com/apikey
GOOGLE_API_KEY=your_google_api_key_here

# =============================================================================
# AGENT CONFIGURATION
# =============================================================================

# Which Gemini model to use for the agent
# Options:
#   - gemini-2.5-flash (recommended: fast, cost-efficient)
#   - gemini-2.5-pro (more powerful reasoning, slower)
GEMINI_MODEL=gemini-2.5-flash

# Which Gemini Live model to use for real-time audio
# Options:
#   - gemini-2.5-flash-native-audio-preview-09-2025 (recommended: lowest latency)
#   - gemini-live-2.5-flash-preview (alternative)
GEMINI_LIVE_MODEL=gemini-2.5-flash-native-audio-preview-09-2025

# Maximum queries per minute per user (rate limiting)
AGENT_MAX_QUERIES_PER_MINUTE=10

# Maximum conversation history turns to keep (token management)
AGENT_MAX_HISTORY_TURNS=50

# Enable Voice Activity Detection (reduces API calls)
AGENT_ENABLE_VAD=true

# Minimum silence duration (seconds) to stop recording
AGENT_VAD_SILENCE_DURATION=1.5

# Default agent mode on startup
# Options: guidance, agent_qa, agent_monitor, agent_nav, agent_contextual, agent_hybrid
AGENT_DEFAULT_MODE=guidance

# =============================================================================
# SYSTEM CONFIGURATION
# =============================================================================

# Flask server host and port
FLASK_HOST=0.0.0.0
FLASK_PORT=5000

# Enable debug mode (development only)
FLASK_DEBUG=false

# =============================================================================
# INSTRUCTIONS
# =============================================================================
# 1. Copy this file to .env
# 2. Replace 'your_google_api_key_here' with your actual API key
# 3. Adjust agent configuration as needed
# 4. Never commit the .env file to version control!
